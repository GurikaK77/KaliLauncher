# KaliLauncher

Git-ready launcher project.

## First run
```bash
npm install
npm start
```

## Build installers
```bash
npm run dist:win
npm run dist:linux
```

## GitHub upload
Make sure `.gitignore` stays in the project root so `node_modules` is not committed.
